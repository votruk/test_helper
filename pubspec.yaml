name: test_helper
description: A new Flutter project.
version: 0.0.1
homepage: https://github.com/votruk/test_helper
publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.9
  shared_preferences: ^2.0.13
  rxdart: ^0.27.3
  mocktail_image_network: ^0.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
